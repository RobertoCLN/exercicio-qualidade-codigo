function calcularAreaRetangulo1(largura: number, altura: number): number {
  return largura * altura;
}

function calcularAreaRetangulo2(largura: number, altura: number): number {
  return largura * altura;
}

function calcularAreaRetangulo3(largura: number, altura: number): number {
  return largura * altura;
}

console.log(calcularAreaRetangulo1(10, 5));
console.log(calcularAreaRetangulo2(7, 3));
console.log(calcularAreaRetangulo3(4, 2));